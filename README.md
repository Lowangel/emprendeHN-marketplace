
  # Marketplace Prototype for EmprendeHN

  ## Running the code
  usar en la ruta principal : PS C:\Users\Angel\Documents\Marketplace Prototype for EmprendeHN> 
  npm run dev

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
